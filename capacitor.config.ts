import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartview.ionic.mobile',
  appName: 'smart-view-ionic-mobile',
  webDir: 'www'
};

export default config;
